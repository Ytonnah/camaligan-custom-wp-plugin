# News JSON Endpoint - COMPLETED ✅

Implementation:
- json_news_list() method added to News_Viewer class ✓
- news_viewer_shortcode handles ?format=json ✓
- Returns JSON array with ID, title, category, priority, date, image, excerpt

Usage: Add [news_viewer] shortcode to page `/news`, access `/news?format=json`

All steps complete. Tests: HTML shows normally, JSON returns data.
